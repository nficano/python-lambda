.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install python-lambda

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-lambda
    $ pip install python-lambda
